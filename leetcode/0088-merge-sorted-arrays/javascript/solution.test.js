const { solution } = require('./solution')



describe('solution function', () => {
  test('should ', () => {
    
    const test = [1,2,2,4]
    expect( solution( test ) ).toEqual( [2, 3] )
  })
  
})

